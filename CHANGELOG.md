# Changelog

## 0.3.0 — configurable fal service

- Add a repeatable fal service with explicit generation and existing-video inputs.
- Package small-mask perspective-video repair with independent top/bottom switches and prompts.
- Return full ERP output, unrepaired comparison and stage-specific receipts.
- Isolate GPU stages for cancellation, memory release and LoRA separation.
- Add service schema, projection/composition tests and a standalone wheel builder.
- Validate both existing-video and full 1536×672 generation-plus-repair paths on fal H200; record imagery, hashes, timing and reviewer-access limitations.

## 0.2.0 — final shifted prediction

- Extract the tested final-step sampler and complete H3 T2V runtime.
- Build the clean wheel into a standalone fal deployment bundle.
- Audit canonical/treatment outputs from the same forward call and check the opposite longitude.
- Add opposite-side report images and a fifth PDF page.
- Start polar coordinate controls and document the next experiment sequence.


## Unreleased - final-prediction research report

- Document the tested final narrow shifted-ERP prediction followed by circular decoding.
- Add matched native-resolution temple/forest evidence and the 100-step timing comparison.
- Include a four-page report card, curated lossless-source images and measurement provenance.
- Decoder package/API unchanged; sampler integration remains experimental and is not packaged.

## 0.1.0 - experimental release candidate

- Horizontal context wrapper for the raw ComfyUI H3 visual VAE: native-width, 128 px and 384 px comparisons.
- Original-canvas crop with explicit shape checks; caller-owned model loading, dtype, inference and memory management.
- Matched forest/temple measurements, weight/latent provenance and seam close-ups.
- Installation instructions, CPU output-buffer example and geometry/contract tests.
- Optional static viewer source. Camera sliders no longer change the selected scene.

No original-code license selected yet. The packaged callable has not been rerun against real H3 weights; the original harness produced the GPU evidence. Pole mappings and alternative periodic tile assembly remain experimental. No upscaling or seam-noise repair claim is included.
